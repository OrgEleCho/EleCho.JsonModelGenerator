# EleCho.JsonModelGenerator

一个 JSON 实体类生成器. (适用于 C#)

## 功能 / Features

- [x] 自动命名风格转换 (帕斯卡, 驼峰, 蛇形)
- [x] 类型嵌套风格自定义 (嵌套, 不嵌套, 不嵌套并优化命名)
- [x] 数组类型自定义 (Array, List&lt;T&gt;, IEnumerable&lt;T&gt;)
- [x] 数组成员类型命名自定义 (单复数转换, 直接使用属性名, 使用属性名 + Item)
- [x] 成员特性添加 (System.Text.Json, Newtonsoft.Json)

